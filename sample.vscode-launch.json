{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch file",
            "type": "go",
            "request": "launch",
            "mode": "test",
            "program": "${fileDirname}",
            "env": {
                "APPLICATION_PROPERTIES_FILE":"application.properties"
            }
        }
    ]
}